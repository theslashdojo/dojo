services:
  registry:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - SKILLS_DIR=/app/nodes,/app/examples
    volumes:
      - ../nodes:/app/nodes:ro
      - ../examples:/app/examples:ro
    restart: unless-stopped
