interface:
  display_name: "Dojo Validate"
  short_description: "Check Dojo manifests for structural and knowledge regressions."
  default_prompt: "Use this skill to validate a Dojo manifest before smoke tests or publishing, and surface both hard errors and agent-facing quality warnings."

policy:
  allow_implicit_invocation: true
