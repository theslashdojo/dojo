interface:
  display_name: "Dojo Knowledge Validate"
  short_description: "Check whether a Dojo node is strong enough for search and learn."
  default_prompt: "Use this skill to inspect discovery signals, section quality, graph links, and actionability in a Dojo node."

policy:
  allow_implicit_invocation: true
