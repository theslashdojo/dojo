interface:
  display_name: "Dojo Schema Validate"
  short_description: "Run hard Dojo manifest checks without knowledge heuristics."
  default_prompt: "Use this skill to catch required-field, hierarchy, URI, and reference integrity errors in a Dojo manifest."

policy:
  allow_implicit_invocation: true
