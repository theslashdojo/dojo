interface:
  display_name: "Dojo Query Client"
  short_description: "Search, inspect, learn, and bundle from a Dojo registry."
  default_prompt: "Use this skill to query a Dojo registry, inspect why nodes matched, load learn payloads, or fetch portable skill bundles from the CLI or JavaScript."

policy:
  allow_implicit_invocation: true
