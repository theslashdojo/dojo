interface:
  display_name: "Dojo Authoring"
  short_description: "Create, scaffold, and enrich Dojo node packages."
  default_prompt: "Use this skill when an agent needs to create a new Dojo node, scaffold the package layout, add executable scripts, or enrich a thin node with searchable knowledge fields before validation."

policy:
  allow_implicit_invocation: true
