interface:
  display_name: "Dojo Knowledge Registry"
  short_description: "Find answers, inspect matches, and export executable Dojo bundles."
  default_prompt: "Use this skill to ask Dojo answer-first questions with `agent_learn`, discover the right node for a task, inspect why a node matched, and fetch a portable skill bundle that can run from JavaScript or the CLI."

policy:
  allow_implicit_invocation: true
