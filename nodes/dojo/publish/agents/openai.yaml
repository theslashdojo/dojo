interface:
  display_name: "Dojo Publish"
  short_description: "Dry-run, rehearse, and publish Dojo manifests."
  default_prompt: "Use this skill to prepare a Dojo release, rehearse the read-side routes, and publish a validated manifest with explicit version discipline."

policy:
  allow_implicit_invocation: true
