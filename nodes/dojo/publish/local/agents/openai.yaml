interface:
  display_name: "Dojo Publish Rehearsal"
  short_description: "Smoke-test Dojo read-side routes before publishing."
  default_prompt: "Use this skill to rehearse a Dojo release by checking search, skill, learn, and bundle routes against a registry."

policy:
  allow_implicit_invocation: true
