interface:
  display_name: "Dojo Registry Publish"
  short_description: "POST a Dojo manifest to a registry with auth."
  default_prompt: "Use this skill to publish a validated Dojo manifest to a registry with bearer auth and machine-readable output."

policy:
  allow_implicit_invocation: true
