interface:
  display_name: "Ethereum Send ETH"
  short_description: "Send native ETH through an Ethereum RPC endpoint."
  default_prompt: "Use this skill when an agent needs to transfer native ETH, inspect required wallet/RPC inputs, reason about nonces and gas, or run the bundled send-eth script."

policy:
  allow_implicit_invocation: true
